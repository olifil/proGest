{# src/proGestBundle/Resources/views/Fournisseur/choiceVariation.html.twig #}


<p>
  J'ai trouvé pour ce produit plusieurs livraison. Merci de sélectionner celle qui doit être modifiée.
</p>
<ul>
  {% for variation in variations %}
    <li id="{{ variation.id }}" data="{{ variation.article.id }}" data-quantite="{{ quantite }}">
      le {{ variation.date|date('d/m/Y') }}, <strong>{{ variation.quantite }}</strong> unité(s) de ce produit ont été livrée.
      &nbsp;<button type="button" class="btn btn-default btn-xs"><span class="fa fa-check-circle-o"></span></button>
    </li>
  {% endfor %}
</ul>
