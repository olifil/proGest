<nav class="navbar navbar-default">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('homepage') }}">
              <img src="{{ asset('bundles/progest/imgs/brand-logo.png')}}" alt="Logo application" />
            </a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">

            {# Lien de menu "La Boutique" #}
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                <span class="fa fa-home fa-fw"></span> La Boutique <span class="caret"></span></a>
              <ul class="dropdown-menu">
                {% if boutique != null %}
                  <li><a href="{{ path('boutique-edit', {'id': boutique.id})}}" title="Edition de la boutique"><span class="fa fa-edit fa-fw"></span> Editer votre boutique</a></li>
                {% else %}
                  <li><a href="{{ path('boutique-add')}}" title="Initialisation de la boutique"><span class="fa fa-file-o fa-fw"></span> Créer une boutique</a></li>
                {% endif %}
                <li role="separator" class="divider"></li>
                {% if boutique != null %}
                  {% if boutique.isactive !=true %}
                    <li>
                      <a id="btnBoutiqueInit" href="{{ path('boutique-init')}}" title="Crée un fichier 'Etat initial' de votre boutique">
                        <span class="fa fa-wrench fa-fw"></span> Initialiser votre boutique
                      </a>
                    </li>
                  {% else %}
                    <li>
                      <a id="btnBoutiqueInitFileView" href="#" title="Afficher le fichier d'initialisation">
                        <span class="fa fa-sign-in fa-fw"></span> Etat initial
                      </a>
                    </li>
                    <li>
                      <a id="btnCloreSaison" href="{{ path('saison-clore')}}" title="Clore la saison">
                        <span class="fa fa-sign-out fa-fw"></span>&nbsp;Clore la saison
                      </a>
                    </li>
                  {% endif %}
                  <li role="separator" class="divider"></li>
                {% endif %}
                <li>
                  <a href="{{ path('backup')}}">
                    <span class="fa fa-save fa-fw"></span> Sauvegarder les données
                  </a>
                </li>
              </ul>
            </li>

            {% if boutique|length != 0 %}
              {% if boutique.IsActive != null %}
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                  <span class="fa fa-shopping-bag fa-fw"></span> Les ventes <span class="caret"></span>
                </a>
                <ul class="dropdown-menu">
                  <li>
                    <a href="{{ path('ventes') }}">
                      <span class="fa fa-calendar fa-fw"></span> Vos ventes
                    </a>
                  </li>
                  <li {% if vente != true %}class="disabled"{% endif %}>
                    <a id="btnDaystate" href="#" title="Editer l'état quotidien">
                      <span class="fa fa-calendar-times-o fa-fw"></span> Vos ventes du jour
                    </a>
                  </li>
                  <li role="separator" class="divider"></li>
                  <li>
                    <a href="{{ path ('vente-new') }}" title="Ajouter une vente">
                      <span class="fa fa-cart-plus fa-fw"></span> Créer une vente
                    </a>
                  </li>
                </ul>
              </li>
              {% endif %}
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                  <span class="fa fa-building-o fa-fw"></span> Les fournisseurs <span class="caret"></span>
                </a>
                <ul class="dropdown-menu">
                  <li>
                    <a href="{{ path('fournisseurs') }}" title="Liste de tous les fournisseurs">
                      <span class="fa fa-list fa-fw"></span> Vos fournisseurs
                    </a>
                  </li>
                  <li role="separator" class="divider"></li>
                  <li>
                    <a href="{{ path ('fournisseur-add') }}" title="Ajouter un fournisseur">
                      <span class="fa fa-plus-square fa-fw"></span> Nouveau fournisseur
                    </a>
                  </li>
                </ul>
              </li>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                  <span class="fa fa-archive fa-fw"></span> Les articles <span class="caret"></span>
                </a>
                <ul class="dropdown-menu">
                  <li>
                    <a href="{{ path('articles') }}" title="Liste de tous les articles">
                      <span class="fa fa-list fa-fw"></span> Vos articles
                    </a>
                  </li>
                  <li role="separator" class="divider"></li>
                  <li>
                    <a href="{{ path ('article-add') }}" title="Ajouter un article">
                      <span class="fa fa-plus-square fa-fw"></span> Nouvel article
                    </a>
                  </li>
                </ul>
              </li>
            {% endif %}
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <li><a href="{{ path('credits')}}">Crédits</a></li>
          </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>
